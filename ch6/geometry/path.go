package geometry

//a Path is a journey between points
type Path []Point

//Distance returns the distance travelled along the Path
func (path Path) Distance() float64 {
	sum := 0.0
	for i := range path {
		if i > 0 {
			sum += path[i-1].Distance(path[i])
		}
	}
	return sum
}